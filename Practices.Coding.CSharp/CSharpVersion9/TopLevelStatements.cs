using System.Text;
namespace Practices.Coding.CSharp.CSharpVersion9;


//A project can have only one Main method. Similary a project can have only one file with top-level statements. 
//Putting top-level statements in more than one file in a project result in compiler error.
//StringBuilder builder = new();
//builder.Append("Hello);
//builder.Append("World!);

//Console.WriteLine(builder.ToString());

/*
 * A file with top-level statements can also contain namespaces and type definitions,
 * but they must come after the top-level statements. 
 * 
 */

// You can call async mehtod by using await.




